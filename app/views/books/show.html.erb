<% if @book_new.errors.any? %>
  <%= @book_new.errors.count %>件のエラーが発生しました
  <ul>
    <% @book_new.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>


<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <h2>User info</h2>
     <table><%= image_tag @user.get_profile_image(100,100) %><table class="table">
  <tbody><tr></tr>
  <tr>
  <th>name</th>
	<th><%= @user.name %></th>
  </tr>
  <tr>
  <th>Introduction</th>
  <p><%= @user.introduction %></p>
  </tr>
  </tbody></table>
   <div class="row" >
  <%= link_to edit_user_path(@user),class:"btn btn-outline-secondary btn-block edit_user_23316" do %>
  <i class ="fas fa-user-cog"></i>
  <% end %>
  </div>

  <form>
      <h2 class="mt-3">New book</h2>
      <%= form_with model: @book_new do |f| %>
 <div class="form-group">
    <label for="book_title">Title</label>
     <%= f.text_field :title %>
  </div>
  <div class="form-group">
    <label for="book_opinion">Opinion</label>
    <%= f.text_area :body %>
  </div>
  <div class="form-group">
    <input type="submit" name="commit" value="Create Book" class="btn btn-success" data-disable-with="Create Book">
  </div>
  <%end%>
</form>
</div>

<div class="col-md-8 offset-md-1">
<h2>Book detail</h2>
<table class="table">
<tbody><tr>
</tbody>
<div class="book-wrap">

<div class="book-box">
<div class="book-comment">
    <tr>
      <td><%= link_to user_path(@user) do %>
  		  <%= image_tag @user.get_profile_image(200,200) %><%end%><br>
	      <%= link_to @user.name, user_path(@user) %></td>
      <td><span class="book-title"><%= link_to @book.title, book_path(@book.id)%></span></td>
      <td><span class="book-body"><%= @book.body %></span></td>
      <% if current_user==@book.user %>
      <td><%= link_to "Edit", edit_book_path(@book),class:"btn btn-sm btn-success edit_book_17748"%></td>
      <td><%= link_to "Destroy" ,book_path(@book),method: :delete,class:"btn btn-sm btn-danger destroy_book_17748","data-confirm"=>"本当に削除しますか？" %></td>
      <%end%>
    </tr>


</div>
</div>

</table>
</div>